# f14-weapons — Gemini CLI Entry Point

このリポジトリはF-14関連素材を保持します。リポジトリ内には非常に大きいbase64テキストがあるため、Geminiはそれらを通常の文脈として読まないでください。

## Token discipline

- `*_b64.txt` はユーザーが明示的に内容処理を依頼した場合を除き、絶対に一括読込しない。
- 素材の存在確認ではファイル名・サイズ・Gitメタデータだけを使う。
- base64を解析目的でプロンプトへ展開しない。
- READMEや小さいメタデータを優先し、必要な対象だけ扱う。
- アーカイブ、依存物、キャッシュ、生成物を探索目的で大量読込しない。

GitHub上のファイルを正本とし、素材そのものを書き換える依頼では対象を明示的に確認してから変更します。
